defaults: &defaults
  app_name: "文档系统"
  domain: "127.0.0.1:3000"
  google_analytics_key: "apikey"
  foot_html: "&copy; API Docs."

development:
  <<: *defaults

test:
  <<: *defaults

production:
  <<: *defaults
