- content_for :styles do
  = stylesheet_link_tag "topics"

.box
  %h3 Docs List
  .topics
    - @docs.each do |doc|
      .topic.topic_line
        .title  #{link_to doc.name, project_doc_path(@project, doc)} #{doc.desc}
        .info.time 最后更新: #{doc.updated_at.to_s(:db)}