= simple_form_for(@project, :url => admin_projects_url) do |f|
  = f.error_notification

  .inputs
    = f.input :name
    = f.input :cname
    = f.input :desc
    = f.input :version

  .actions
    = f.button :submit
