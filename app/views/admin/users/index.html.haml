%h3 用户管理

%table
  %tr
    %th 用户
    %th 邮箱
    %th 角色
    %th 项目
    - if current_user.master?
      %th 操作

  - @users.each do |user|
    %tr
      %td= link_to user.name, user
      %td= user.email
      %td= user.role
      %td= user.projects.join(', ')
      - if current_user.master?
        %td #{link_to '编辑', edit_admin_user_path(user)} | #{link_to '删除', user, :confirm => '你确定删除吗?', :method => :delete}

%br
