!!!
%html
  %head
    %title API Wiki
    %meta{:charset => "utf-8"}
    %meta{:content => "", :name => "description"}
    %meta{:content => "", :name => "author"}

    / Le HTML5 shim, for IE6-8 support of HTML elements
    /[if lt IE 9]
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
    = stylesheet_link_tag    "fluid"
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body
    .topbar
      .topbar-inner
        .container-fluid
          %a.brand{:href => "/"} 易分享#{@project.version.capitalize}
          %ul.nav
            %li.active
              = link_to 'API文档', project_docs_path(@project)
            %li
              = link_to '关于'
          %p.pull-right
            %span= link_to(current_user.name, edit_user_registration_path)
            |
            %span= link_to('Logout', destroy_user_session_path, :confirm => "Are you sure to logout?",  :method=>'delete')
    .container-fluid
      .sidebar
        .well
          %h5 分类
          %ul
            - @project.categories.each do |c|
              %li
                %a{:href => "#"} #{c.name}
          %h5 达人
          %ul
            %li
              %a{:href => "#"} 关注
            %li
              %a{:href => "#"} 粉丝
            %li
              %a{:href => "#"} 私信
          %h5 动态
          %ul
            %li
              %a{:href => "#"} 全部
            %li
              %a{:href => "#"} 好友
            %li
              %a{:href => "#"} 自己
      - flash.each do |name, msg|
        = content_tag :div, :class => "container alert-message #{name == :error ? "error" : "success" }", :"data-alert" => "alert" do
          %a.close{:href => "#"} ×
          %p= msg
      .content
        .row
          .span16
            = yield
        %footer
          / %p © Company 2011 (Wiki)
